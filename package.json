{
  "name": "hacklab-prototype",
  "version": "1.0.0",
  "description": "Hacklab Prototype",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node api/bin/www",
    "build": "cd api && npm i && cd ../client && npm i && npm run build && cp -R build/ ../api/public "
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  }
}
